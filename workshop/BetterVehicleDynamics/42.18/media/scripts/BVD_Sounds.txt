module Base
{
    // v0.1.6: renamed from BVD_SkidLoop -> BVD_SkidTile. PZ's audio engine
    // caches sound-script registrations across world loads; a script-only
    // change from loop=true to loop=false on the SAME event name often does
    // NOT flush the cached loop=true property, leaving heartbeat re-arms
    // permanently looping at each world position. A new event name forces
    // fresh registration with the correct loop=false from the start.
    sound BVD_SkidTile
    {
        category = Object,
        loop = false,
        is3D = true,
        master = VehicleEngine,
        clip { file = media/sound/BVD_skid_loop.ogg, distanceMin = 8, distanceMax = 35, volume = 1.0, }
    }
}
